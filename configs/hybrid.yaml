experiment:
  name: hybrid_baseline_v1

retrieval:
  type: hybrid
  rrf_k: 60
  dense_fetch_k: 10
  bm25_fetch_k: 10

paths:
  predictions_output: experiments/results/hybrid_predictions.jsonl
  retrieval_eval_output: experiments/results/hybrid_retrieval_eval.jsonl
  retrieval_summary_output: experiments/results/hybrid_retrieval_summary.json
