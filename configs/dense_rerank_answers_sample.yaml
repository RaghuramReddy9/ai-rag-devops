experiment:
  name: dense_rerank_answers_sample_v1
  max_questions: 5

retrieval:
  type: dense_rerank
  top_k: 5
  fetch_k: 10
  reranker_model_name: cross-encoder/ms-marco-MiniLM-L6-v2
  reranker_max_length: 512

generation:
  enabled: true

paths:
  predictions_output: experiments/results/dense_rerank_answers_sample.jsonl
